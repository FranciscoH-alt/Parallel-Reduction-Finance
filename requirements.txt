# System-level requirements for building the Parallel Reduction project

cmake>=3.22
gcc>=11.0
g++>=11.0
cuda>=12.0
nvidia-driver>=525

# Optional: If profiling or benchmarking is used
nvprof
nsight-systems
nsight-compute
